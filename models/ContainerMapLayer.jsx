export default class ContainerMapLayer {
    constructor(pointsOfInterest, colorOfPoints, pointType, pointSize) {
        this.pointsOfInterest = pointsOfInterest;
        this.colorOfPoints = colorOfPoints;
        this.pointType = pointType;
        this.pointSize = pointSize;
    }
}
